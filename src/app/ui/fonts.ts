import { Inter, Open_Sans, Urbanist } from "next/font/google";

export const inter = Inter({subsets: ['latin']});

export const open_Sans = Open_Sans({
    weight: ['700'],
    subsets: ['latin']
});

export const urbanist = Urbanist({
    weight:['400', '700'],
    subsets: ['latin']
});


